import { SearchAndFilterCandidatePipe } from './search-and-filter-candidate.pipe';

describe('SearchAndFilterCandidatePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchAndFilterCandidatePipe();
    expect(pipe).toBeTruthy();
  });
});
